{
  "name": "spec-injector",
  "version": "0.1.0",
  "description": "Deterministic task package generator from GitHub issues",
  "type": "module",
  "files": [
    "dist",
    "bin",
    "presets",
    "README.md"
  ],
  "bin": {
    "spec": "./bin/spec.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "pnpm build && node --test tests/cli.integration.test.js",
    "dev": "tsc --watch",
    "start": "node dist/cli/index.js"
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
